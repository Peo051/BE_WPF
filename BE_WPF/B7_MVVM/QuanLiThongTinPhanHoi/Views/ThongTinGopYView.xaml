<UserControl x:Class="QuanLiThongTinPhanHoi.Views.ThongTinGopYView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="#EEF1F5">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding TenKhachHang}"
                   Grid.Row="0"
                   HorizontalAlignment="Right"
                   FontWeight="Bold"
                   FontSize="34"
                   Foreground="#1C73C6"
                   Margin="0,0,0,8"/>

        <TextBlock Grid.Row="1"
                   Text="NỘI DUNG GÓP Ý"
                   HorizontalAlignment="Center"
                   FontSize="40"
                   FontWeight="Bold"
                   Foreground="#0D1533"
                   Margin="0,20,0,18"/>

        <TextBox Grid.Row="2"
                 Height="210"
                 Margin="20,0,20,24"
                 VerticalScrollBarVisibility="Auto"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 FontSize="20"
                 Text="{Binding NoiDungGopY, UpdateSourceTrigger=PropertyChanged}"/>

        <Button Grid.Row="3"
                Content="Gửi góp ý"
                Width="220"
                Height="46"
                VerticalAlignment="Top"
                HorizontalAlignment="Center"
                Command="{Binding GuiGopYCommand}"/>
    </Grid>
</UserControl>
