<UserControl x:Class="QuanLiThongTinPhanHoi.Views.ThongTinKhachHangView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>

        <Label Grid.Column="1" Content="THÔNG TIN KHÁCH HÀNG"
               FontSize="24"
               FontWeight="Bold"
               HorizontalAlignment="Left"
               Margin="0,30,0,0"
               Foreground="DarkBlue"/>

        <StackPanel Grid.Row="1"
                    HorizontalAlignment="Right"
                    Margin="0,0,50,0"
                    VerticalAlignment="Top">
            <TextBlock Text="Tên khách hàng:"/>
            <TextBlock Text="Số điện thoại:" Padding="10"/>
        </StackPanel>

        <StackPanel Grid.Column="1" Grid.Row="1">
            <TextBox Width="500"
                     HorizontalAlignment="Left"
                     Text="{Binding TenKhachHang, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBox Width="500"
                     HorizontalAlignment="Left"
                     Margin="0,5,0,0"
                     Text="{Binding SoDienThoai, UpdateSourceTrigger=PropertyChanged}"/>

            <WrapPanel Margin="0,10,0,0">
                <Button Content="Tham gia góp ý"
                        Margin="0, 20"
                        Height="25"
                        Width="100"
                        Command="{Binding ThamGiaGopYCommand}"/>
                <Button Content="Phản hồi"
                        Margin="30, 20"
                        Height="25"
                        Width="100"
                        Command="{Binding PhanHoiCommand}"/>
                <Button Content="Thoát"
                        Margin="5, 0"
                        Height="25"
                        Width="100"
                        Command="{Binding ThoatCommand}"/>
            </WrapPanel>
        </StackPanel>
    </Grid>
</UserControl>
