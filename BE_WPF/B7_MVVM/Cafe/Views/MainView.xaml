<Window x:Class="Cafe.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Cafe.ViewModels"
        mc:Ignorable="d"
        Title="MainView" Height="450" Width="800">
    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>

    <!--❌ Set DataContext trong UserControl:   → Mỗi màn hình 1 dữ liệu riêng

    ✅ Set trong MainViewModel:    → Tất cả dùng chung dữ liệu 
    UserControl KHÔNG tự tạo ViewModel : MainViewModel quyết định ViewModel-->

    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="Menu">
                <MenuItem Header="Lập hóa đơn" Command="{Binding ShowLapHoaDonCommand}"/>
                <MenuItem Header="Danh sách" Command="{Binding ShowDanhSachCommand}"/>
            </MenuItem>
        </Menu>

        <ContentControl Content="{Binding CurrentView}"/>
    </DockPanel>
</Window>
