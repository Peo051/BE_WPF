<UserControl x:Class="Bai3.Views.ThongKeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800"
             Background="White">
    <StackPanel Margin="10">
        <TextBlock Text="THỐNG KÊ CUỐI NGÀY"
                   FontSize="22" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <Button Content="Tổng kết" Command="{Binding ThongKeCommand}" Width="120" Margin="0,0,0,15"/>

        <StackPanel Orientation="Horizontal" Margin="0,5">
            <TextBlock Text="Số lượt người:" Width="120"/>
            <TextBox Width="200" IsReadOnly="True"
                     Text="{Binding TongSoLuotNguoi}"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,5">
            <TextBlock Text="Tổng số tiền:" Width="120"/>
            <TextBox Width="200" IsReadOnly="True"
                     Text="{Binding TongSoTien, StringFormat={}{0:N0} VND}"/>
        </StackPanel>

        <DataGrid ItemsSource="{Binding DanhSachPhieu}"
                  AutoGenerateColumns="False"
                  Margin="0,20,0,0"
                  Height="250">
            <DataGrid.Columns>
                <DataGridTextColumn Header="STT" Binding="{Binding STT}" Width="50"/>
                <DataGridTextColumn Header="Họ tên" Binding="{Binding KhachHang.HoTen}" Width="150"/>
                <DataGridTextColumn Header="Địa chỉ" Binding="{Binding KhachHang.DiaChi}" Width="150"/>
                <DataGridTextColumn Header="Số ngày" Binding="{Binding SoNgayO}" Width="80"/>
                <DataGridTextColumn Header="Loại phòng" Binding="{Binding LoaiPhong}" Width="120"/>
                <DataGridTextColumn Header="Tiện nghi" Binding="{Binding HienThiTienNghi}" Width="150"/>
                <DataGridTextColumn Header="Dịch vụ" Binding="{Binding HienThiDichVu}" Width="120"/>
                <DataGridTextColumn Header="Thành tiền" Binding="{Binding TongTien, StringFormat={}{0:N0} VND}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
</UserControl>

